package se.umu.cs.ads;

public interface ServiceInterface {
    /*
        // Pod management
        void start(Container container);
        void stop(String podName);
        void restart(String podName);
        void remove(String podName);
        ArrayList<Container> listActiveContaines();
        ArrayList<String> getContainerLogs(String podName);

        // Cluster info
        ArrayList<Node> listActiveNodes();
        ArrayList<Container> listActiveContainers();
        Usage getUsage(Node node);


        // Cluster management
        Node createContainer(Container container);
        Node createContainer(Container container, Node node);
        Node moveContainer(Container container);
        Node moveContainer(Container container, Node node);
        void leaveCluster();
        void joinCluster(String cluster);  //Maybe not
    */
}
